# 📋 アプリケーション テスト仕様書 ＆ 実行結果レポート

**対象アプリケーション：** 一人暮らし向け献立管理・提案アプリ  
**テスト実行日：** 2026年7月11日  
**判定基準：** `〇` = 期待通りに動作（合格） / `✕` = 期待に反する挙動・未実装（不合格）

---

## 1. 正常系テスト（10ケース）
日常的な使われ方や、基本機能が正しく動作するかを検証します。

| ID | テスト分類 | テスト項目（操作・条件） | 期待される振る舞い（正解） | 実行結果 |
| :--- | :--- | :--- | :--- | :---: |
| **TC-01** | 正常系 | 予算400円、和食、2品で「提案してもらう」を押下 | 400円以下の和食（主菜1＋副菜1）が選ば地、合計金額が正しく表示されること。 | **〇** |
| **TC-02** | 正常系 | 品数を「主菜のみ」にして提案を押下 | 主菜1品のみが選ばれ、副菜は非表示、合計食費が主菜のみの価格になること。 | **〇** |
| **TC-03** | 正常系 | 提案された画面で「この献立を記録する」を押下 | 表示内容が `meals.csv` に追記され、下の履歴テーブルに即座に反映されること。 | **〇** |
| **TC-04** | 正常系 | ジャンルを「洋食」、2品で提案を押下 | 予算内の洋食の主菜と副菜がランダムに選ばれ、正しく画面に表示されること。 | **〇** |
| **TC-05** | 正常系 | カレンダーから「昨日（過去）」の日付を選んで記録 | 選択した過去の日付で履歴に正しく登録されること（遡り管理）。 | **〇** |
| **TC-06** | 正常系 | カレンダーから「明日（未来）」の日付を選んで記録 | 選択した未来の日付で履歴に正しく登録されること（予定管理）。 | **〇** |
| **TC-07** | 正常系 | 初期状態（画面を初めて開いた時）の表示確認 | 右側エリアに「条件を入力して「提案してもらう」ボタンを押してください。」とあること。 | **〇** |
| **TC-08** | 正常系 | 複数回「提案してもらう」を連続で押下 | 毎回ランダムに異なるメニューの組み合わせが再提案されること。 | **〇** |
| **TC-09** | 正常系 | アプリ起動時に `meals.csv` が存在しない状態にする | 起動時にファイルが自動生成され、ヘッダー行が正しく書き込まれること。 | **〇** |
| **TC-10** | 正常系 | 最新の食事履歴と同じ主菜が連続提案されないか検証 | 履歴の一番上にある主菜と同一のメニューは、次の提案候補から除外されること。 | **〇** |

---

## 2. 境界値テスト（6ケース）
システムの制限値や、条件の切り替わり目（バグが最も起きやすい部分）を検証します。

| ID | テスト分類 | テスト項目（操作・条件） | 期待される振る舞い（正解） | 実行結果 |
| :--- | :--- | :--- | :--- | :---: |
| **TC-11** | 境界値 | 予算にレシピの最低価格と「同額（例：300円）」を入力 | ぴったり300円のメニュー（または組み合わせ）が存在する場合、正しく提案されること。 | **〇** |
| **TC-12** | 境界値 | 予算にレシピの最低価格「-1円（例：299円）」を入力 | 予算を下回るメニューがないため、安全に「予算に合う献立が見つかりませんでした」となること。 | **〇** |
| **TC-13** | 境界値 | 非常に高い予算（例：999,999円）を入力 | オーバーフローを起こさず、登録されている高価格帯のメニューも含めて提案されること。 | **〇** |
| **TC-14** | 境界値 | 主菜の選択肢が「歴史上1つ（前日と同じもの）」しかない場合 | 前日除外ロジックを安全にバイパスし、クラッシュせずにその1品を提案すること。 | **〇** |
| **TC-15** | 境界値 | 副菜の登録数が「0件」のジャンルで2品提案を実行 | クラッシュせず、主菜のみを抽出して合計金額を計算し、画面に提示すること。 | **〇** |
| **TC-16** | 境界値 | 提案ロジックの最大ランダム試行（50回）の境界検証 | 50回探しても予算内に収まらない場合、無限ループにならず安全に検索を打ち切ること。 | **〇** |

---

## 3. 異常系テスト（8ケース）
ユーザーの誤操作や、破損したデータがシステムに入ってきた際の堅牢性を検証します。

| ID | テスト分類 | テスト項目（操作・条件） | 期待される振る舞い（正解） | 実行結果 |
| :--- | :--- | :--- | :--- | :---: |
| **TC-17** | 異常系 | 予算入力欄を完全に「空文字（未入力）」にして提案 | サーバーが500エラー（クラッシュ）を起こさず、デフォルトの400円として処理されること。 | **〇** |
| **TC-18** | 異常系 | 予算入力欄に「半角スペースのみ」を入力して提案 | トリム処理（空白削除）され、空文字と同様にデフォルトの400円として安全に処理されること。 | **〇** |
| **TC-19** | 異常系 | 予算入力欄に「五百円」などの全角文字列を入力 | 型変換エラー（`ValueError`）を内部でキャッチし、400円にフォールバックすること。 | **〇** |
| **TC-20** | 異常系 | 予算入力欄に「-500」などのマイナス整数を入力 | マイナス予算では合計金額が以下にならないため、安全に「見つかりませんでした」と表示されること。 | **〇** |
| **TC-21** | 異常系 | `recipes.csv` の価格欄に「時価」などの文字が混入 | アプリ起動時や読み込み時にクラッシュせず、該当行のみを安全にスキップすること。 | **〇** |
| **TC-22** | 異常系 | 食事履歴（`meals.csv`）をExcel等で開いてロック中に記録 | ファイル書き込みエラー（`PermissionError`）でクラッシュせず、エラーハンドリングされること。 | **✕** |
| **TC-23** | 異常系 | カレンダーの日付の文字列をブラウザ側で完全に空にして記録 | 本日の日付が自動的にフォールバックとして補完され、CSVに書き込まれること。 | **〇** |
| **TC-24** | 異常系 | `recipes.csv` のヘッダー列の順番がバラバラな場合 | `DictReader` により列順に依存せず、`price` や `menu_name` のキー名で正しく読み込めること。 | **〇** |